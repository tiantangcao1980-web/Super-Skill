# Design Context

Use a quiet operational product register: clear hierarchy, neutral surfaces,
semantic accent color, and restrained density.

Tokens live in `design/tokens.json`. The dashboard should prefer system UI,
stable grid tracks, modest borders, and readable spacing over decorative
gradients, nested cards, or novelty motion.
