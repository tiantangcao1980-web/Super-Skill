# Design Quality

DesignDNA is the source of truth for brand colors, spacing, radius, and component behavior.

The frontend quality gate checks accessibility labels, responsive constraints, stable dimensions, and a quiet product UI that avoids decorative excess.

Acceptance: the rendered dashboard must expose brand context, use real tokens, and pass deterministic quality checks.
