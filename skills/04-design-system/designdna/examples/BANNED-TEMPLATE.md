# [Brand] — BANNED Patterns

> This file complements `DESIGN.md` (positive spec) with negative rules — the specific things this brand **never** does. Load it alongside DESIGN.md and the universal `designdna/anti-slop/SKILL.md`.

## Typography
- NEVER use: [list fonts that contradict the brand, e.g. Times New Roman, Comic Sans, or Inter if the brand uses a different default]
- NEVER use: [font weights the brand avoids]
- NEVER mix: [typographic combinations the brand never does, e.g. serif + script]

## Color
- NEVER use: [specific hex codes that feel wrong for this brand]
- NEVER use: [color roles the brand avoids — e.g. "neon accents," "pastels," "gradients"]
- NEVER pair: [color combinations that feel off-brand]

## Layout
- NEVER use: [layout patterns this brand explicitly avoids]
- NEVER: [specific composition rules — e.g. "3-column card grids," "centered CTAs"]

## Components
- NEVER use: [component patterns off-brand]
- NEVER style buttons as: [e.g. "gradient-filled rounded pills"]
- NEVER style cards as: [e.g. "frosted glass with colored border glow"]

## Motion
- NEVER: [motion patterns off-brand — e.g. "heavy parallax," "bouncy springs"]
- NEVER exceed: [motion intensity limits — e.g. "300ms transitions"]

## Copy
- NEVER use: [placeholder content the brand would never ship]
- NEVER use tone: [e.g. "hype," "corporate speak," "emoji-heavy"]

## Signature "don'ts"
- [One or two patterns specific to this brand that are instantly recognizable as wrong. E.g. for Apple: "NEVER use decorative borders around product shots"]
